# Secrets — NEVER commit real keys. Use GitHub Actions secrets instead.
.env
.env.local

# Python
__pycache__/
*.py[cod]
# local diagnostic / smoke scripts (e.g. _diag_llm.py, _smoke.py) — never commit
_*.py
*.egg-info/
.venv/
venv/
env/

# Reports / logs the bot may write locally
forecast_reports/
*.log

# OS / editor
.DS_Store
.idea/
.vscode/
